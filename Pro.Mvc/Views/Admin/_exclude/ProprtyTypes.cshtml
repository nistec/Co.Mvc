@model IEnumerable<Natam.Data.Entities.PropertyView>
@using GridMvc.Html
@{
    Layout = "~/Views/Admin/_ViewAdmin.cshtml";
    Page.Title = "Building";
    //var DB = Database.Open("cnn_Trace");
    //var Grid = new WebGrid(DB.Query("SELECT * FROM Trace_Act"));
}
@section head {
   <link rel="stylesheet" href="~/Content/css/Gridmvc.css">
   <script type="text/javascript" src="~/Scripts/plagins/gridmvc.min.js"></script>
}


<hgroup class="title">
    <h3 class="rightPan">סוגי נכסים.</h3>
</hgroup>
<div class="rightPan">
    <ul class="toolbar">
        <li>
        <input type="search" placeholder="חיפוש" /></li>
        <li><a href="#">רענון</a></li>
    </ul>
</div>
<div class="clear"></div>

<div class="global-view">
    <div class="container">

        <div dir="rtl" style="clear: both">

            @Html.Grid(Model).Columns(columns =>
                    {
                        columns.Add(c => c.Property_ID).Titled("קוד סוג");
                        columns.Add(c => c.Property_Name).Titled("סוג הנכס");
                        columns.Add(c => c.Kategory).Titled("קטגוריה");
                        columns.Add(c => c.Area_Type).Titled("סוג אזור");
                    }).WithPaging(15).Sortable().Filterable().WithMultipleFilters()


@*
            @Grid.GetHtml(
        tableStyle: "MyTable",
        headerStyle: "columnHead",
        alternatingRowStyle: "altRow",
        columns: Grid.Columns(
        Grid.Column("RowId", "קוד נכס"),
                    //Grid.Column("Related", "Related"),
                    //Grid.Column("Mode", "Mode"),
                    Grid.Column("Method", "תאור"),
                    //Grid.Column("ActTime", "Act Time"),
                    Grid.Column("Status", "קטגוריה")
                    
                    
        )
    )*@
        </div>

    </div>
</div>

